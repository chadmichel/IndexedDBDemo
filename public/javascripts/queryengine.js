var QueryEngine;
(function (CommandEngine) {
    
    QueryEngine.Engine = function() {
        var self = this;
        
        self.GetAll = function() {
            
        }
    
    }
    
})(QueryEngine || (QueryEngine = {}));

var commandEngine = new QueryEngine.Engine();